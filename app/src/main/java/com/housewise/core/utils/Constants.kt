package com.housewise.core.utils

object Constants {
    // Note: Since this is HTTP (not HTTPS), you need to allow cleartext traffic in your Manifest.
    const val BASE_URL = "http://3.111.4.8:8181/"
}